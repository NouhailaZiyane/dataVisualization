@model IEnumerable<googleCharts.Models.dbfiles>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tabid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.datafile)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.date_envoi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.flg)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.acteurtid)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tabid)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.datafile)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.date_envoi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.flg)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.acteurtid)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.tid }) |
            @Html.ActionLink("Details", "Details", new { id=item.tid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.tid })
        </td>
    </tr>
}

</table>
